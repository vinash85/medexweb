services:
  derm-mcp:
    build: .
    image: derm-mcp
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    volumes:
      - ./models:/models
      - ./data:/data
